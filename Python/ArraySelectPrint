#Prints the range of elemets in array index e.g. 5 - 17 will show 5th to 17th element (spaces included)
colors = ["red", "red", "green", "blue"]
sampleText = 'Name Ricardo Darín'

for color in colors:
    if color == color:
        continue
    print(color)

print(sampleText[5:18]) #Should only print "Ricardo Darín"